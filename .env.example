# 🔐 SEGURANÇA E CONFIGURAÇÃO
# Renomeie este arquivo para .env antes de rodar o projeto

# 1. Banco de Dados (PostgreSQL)
# Exemplo local: postgresql://usuario:senha@localhost:5432/nome_do_banco
DATABASE_URL="postgresql://postgres:password@localhost:5432/ngo_management"

# 2. Autenticação (NextAuth.js)
# Gere um segredo seguro com: openssl rand -base64 32
AUTH_SECRET="seusegredomuitoseguroaqui123456789"

# URL da aplicação (em produção, use o domínio real)
AUTH_URL="http://localhost:3000"

# 3. Ambiente (development, production, test)
NODE_ENV="development"
